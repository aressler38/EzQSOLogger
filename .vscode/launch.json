{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start App",
      "type": "coreclr",
      "request": "attach",
      "preLaunchTask": "RUN APP",
      "processId": "${command:azureFunctions.pickProcess}"
    },
    {
      "name": "Attach to .NET Functions",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}